{
  "_id": "_design/event",
  "_rev": "4-ba9be2451f88a4562a8bf4fa7e763ea6",
  "views": {
    "byPhonenumber": {
      "map": "function (doc) { if (doc.type == 'event') { emit(doc.phonenumber, {_id: doc._id, state: doc.state}) } } "
    },
    "all": {
      "map": "function (doc) { if (doc.type == 'event'){ emit([doc._id], doc); } else if (doc.type == 'vote') { emit([doc.event_id, doc.vote, doc._id], doc); } }",
      "reduce": "_count"
    },
    "votes": {
      "map": "function (doc) { if (doc.type == 'vote') { emit([doc.event_id, doc._id], doc.event_id); }}"
    },
    "list": {
      "map": "function (doc) { if (doc.type == 'event'){ emit([doc._id], doc); } }"
    },
    "voters": {
      "map": "function (doc) { if (doc.type == 'vote') { emit([doc.event_id, doc._id], doc); }}"
    },
    "votingEvents": {
      "map": "function (doc) { if (doc.type == 'votingEvent') { emit([doc.event_id, doc._id], doc); }}"
    }
  }
}
